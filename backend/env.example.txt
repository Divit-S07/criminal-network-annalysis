# SIH Criminal Network Analysis — Backend environment variables
# Copy these lines into a file named "backend/.env" and fill in real values.

PORT=3000
NODE_ENV=development

# Auth
JWT_SECRET=change-this-to-a-long-random-secret
JWT_EXPIRY=7d
BCRYPT_ROUNDS=10

# Gemini — the ONLY external AI. Free key: https://aistudio.google.com/apikey
GEMINI_API_KEY=replace-with-your-gemini-api-key
GEMINI_MODEL=gemini-2.0-flash
GEMINI_FALLBACK_MODEL=gemini-1.5-flash

# CORS / frontend
FRONTEND_URL=http://localhost:5173

# Uploads
MAX_FILE_SIZE_MB=10
UPLOAD_PATH=./uploads
ALLOWED_EXTENSIONS=pdf,docx,csv,jpg,jpeg,png
